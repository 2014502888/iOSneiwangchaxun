name: Build iOS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Setup XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: cd MailTracker && xcodegen generate

      - name: Build
        run: |
          cd MailTracker
          xcodebuild -project MailTracker.xcodeproj \
            -scheme MailTracker \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=""
          ls build/Build/Products/Release-iphoneos/MailTracker.app/
          ls build/Build/Products/Release-iphoneos/MailTracker.app/Frameworks/ || echo "No Frameworks"

      - name: Package IPA
        run: |
          cd MailTracker/build/Build/Products/Release-iphoneos
          mkdir -p Payload
          cp -r MailTracker.app Payload/
          zip -r ../../../../MailTracker.ipa Payload
          ls -la ../../../../MailTracker.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: MailTracker-IPA
          path: MailTracker/MailTracker.ipa
